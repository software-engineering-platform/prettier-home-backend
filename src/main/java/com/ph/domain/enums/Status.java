package com.ph.domain.enums;

public enum Status {
    /*
    Bu enum, 4 farklı tur durumu içerir: PENDING (Beklemede), APPROVED (Onaylı),DECLINE(Reddedildi) CANCELLED (İptal Edildi)
     */
    PENDING,
    APPROVED,
    DECLINED,
    CANCELED
}
